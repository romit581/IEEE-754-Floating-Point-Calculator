
# Server Configuration
server.port=8080

# Application Name
spring.application.name=IEEE754-Calculator

# Logging Configuration
logging.level.root=INFO
logging.level.com.ieee754=DEBUG

# JSON Configuration
spring.jackson.serialization.indent-output=true

# CORS Configuration (if needed)
# Uncomment if you want more control over CORS
# spring.web.cors.allowed-origins=http://localhost:3000,http://127.0.0.1:5500
# spring.web.cors.allowed-methods=GET,POST,OPTIONS
# spring.web.cors.allowed-headers=*